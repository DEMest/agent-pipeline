def test_environment_works():
    """Проверяет, что окружение проекта живо: интерпретатор, зависимости, раннер тестов.

    Нужен, чтобы CI был зелёным с первого дня, до появления первой фичи.
    Красный пайплайн, который «починим, когда будет настоящий код», не чинится никогда.
    """
    assert 1 + 1 == 2
