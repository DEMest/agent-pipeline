package main

import "testing"

// Нужен, чтобы CI был зелёным с первого дня, до появления первой фичи:
// красный пайплайн, который «починим потом», не чинится никогда.
func TestEnvironmentWorks(t *testing.T) {
	if 1+1 != 2 {
		t.Fatal("окружение проекта неисправно")
	}
}
